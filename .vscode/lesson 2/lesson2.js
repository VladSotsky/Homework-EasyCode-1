/*let car = "skoda,toyota";
let speed = 180;
let carspeed="Всем мои машины "+(car)+" ездят со скоростью"+(speed);
let Arrive =[`Все мои машины ${car}ездят со скорсотью ${speed}`];
alert (Arrive);
alert (carpeed);
developer mazila
Метод slice возвращает часть строки.
*/

//let myName='vLAD';

//alert (myName[0].toUpperCase()+myName.slice(1).toLowerCase());

/*let test =['JaScript'];
let testItem1 = test[0];
alert(testItem1.slice(0,2)+'va'+testItem1.slice(2))
alert (test);*/

// === делает приведение к типам .(Сравнивает тип переменных).
 // + вначале строки приводит ее к числу
 //let Year =+"2015";
//alert (Year % 20);
// alert (!0)
//Math.max(...[25,1,3,10]);
//Math.max(25,1,3,10);
// Math.pow() - возводит в степень
// Math.round() - Сокращает в большую сторону
// Math.floor() - Сокращает в меньшую сторону
// Math.abs() - Возвращает только положительное число
 //alert ( Math.random()*100  ) //- Любое число от 0 до 1
// Math.max(), Math.min() - Максимальное и минимальное число из переданных параметров
//(typeoff)-проверяет тип переменной!!!!
// let myObject = {name :'Oleg'}
// myObject.age = 21
// myObject[skill]='normal';
// console.log (myObject);
